import styled from "styled-components/native";
import { Text } from "react-native-paper";
import { View } from "react-native";

export const StatusSwitchLabel = styled(Text)`
	margin-right: 10px;
`;

export const StatusSwitchContainer = styled(View)`
	flex-direction: row;
`;
