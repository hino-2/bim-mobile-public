import axios from "axios";
import { GetUsersResponse, User, GetUserResponse } from "./types";

const usersAPI = {
	getList: `classified`,

	add: `classified`,
	edit: `classified`,
	remove: `classified`,
};

export default usersAPI;
